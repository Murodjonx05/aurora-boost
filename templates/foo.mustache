<!-- === MODERN BLACK FOOTER | SAFE, NO SCROLL-X, CONTAINER BASED === -->
<footer id="aurora-footer">
  <style>
    /* ===== ROOT ===== */
    #aurora-footer {
      --panel: #121216;
      --text: #e5e7eb;
      --muted: #9ca3af;
      --accent: #38bdf8;
      --border: rgba(255,255,255,0.08);

      width: 100%;
      background:
        radial-gradient(1200px 400px at 50% -200px, rgba(56,189,248,0.08), transparent 60%),
        linear-gradient(180deg, #0b0b0d 0%, #09090b 100%);
      color: var(--text);

      font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Arial, sans-serif;
      box-sizing: border-box;
    }

    #aurora-footer *,
    #aurora-footer *::before,
    #aurora-footer *::after {
      box-sizing: inherit;
    }

    /* top divider */
    #aurora-footer::before {
      content: "";
      display: block;
      height: 1px;
      background: linear-gradient(
        90deg,
        transparent,
        rgba(56,189,248,0.35),
        transparent
      );
    }

    #aurora-footer a {
      color: var(--text);
      text-decoration: none;
      transition: color .2s ease;
    }

    #aurora-footer a:hover {
      color: var(--accent);
    }

    /* ===== CONTAINER ===== */
    #aurora-footer .af-container {
      max-width: 1280px;
      margin: 0 auto;
      padding: 56px 24px 32px;
      width: 100%;
    }

    /* ===== GRID ===== */
    #aurora-footer .af-grid {
      display: grid;
      grid-template-columns: 1.2fr 1fr 1fr;
      gap: 32px;
    }

    @media (max-width: 900px) {
      #aurora-footer .af-grid {
        grid-template-columns: 1fr;
      }
    }

    /* ===== BRAND ===== */
    #aurora-footer .af-brand {
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    #aurora-footer .af-logo {
      display: flex;
      align-items: center;
      gap: 14px;
    }

    #aurora-footer .af-logo img {
      width: 48px;
      height: 48px;
      object-fit: contain;
      border-radius: 10px;
      background: #000;
      display: block;
    }

    #aurora-footer .af-title {
      font-size: 18px;
      font-weight: 600;
      letter-spacing: 0.3px;
    }

    #aurora-footer .af-desc {
      color: var(--muted);
      line-height: 1.6;
      max-width: 420px;
    }

    /* ===== SECTIONS ===== */
    #aurora-footer .af-section h4 {
      font-size: 14px;
      font-weight: 600;
      margin-bottom: 14px;
      color: #f9fafb;
      letter-spacing: 0.4px;
    }

    #aurora-footer .af-list {
      display: flex;
      flex-direction: column;
      gap: 10px;
      font-size: 14px;
      color: var(--muted);
    }

    #aurora-footer .af-list a {
      color: var(--muted);
    }

    #aurora-footer .af-list a:hover {
      color: var(--accent);
      text-decoration: underline;
    }

    /* ===== SOCIALS ===== */
    #aurora-footer .af-socials {
      display: flex;
      gap: 12px;
      margin-top: 12px;
    }

    #aurora-footer .af-social {
      width: 40px;
      height: 40px;
      border-radius: 12px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      background: var(--panel);
      border: 1px solid var(--border);
      transition: transform .2s ease, box-shadow .2s ease, border .2s ease;
    }

    #aurora-footer .af-social:hover {
      transform: translateY(-2px);
      border-color: rgba(56,189,248,.4);
      box-shadow:
        0 0 0 1px rgba(56,189,248,.35),
        0 8px 24px rgba(56,189,248,.15);
    }

    #aurora-footer .af-social svg {
      width: 18px;
      height: 18px;
      fill: #e5e7eb;
      display: block;
    }

    /* ===== BOTTOM ===== */
    #aurora-footer .af-bottom {
      margin-top: 40px;
      padding-top: 18px;
      border-top: 1px solid var(--border);
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 12px;
      font-size: 13px;
      color: var(--muted);
    }
  </style>

  <div class="af-container">
    <div class="af-grid">
      <!-- BRAND -->
      <div class="af-brand">
        <div class="af-logo">
          <img src="LOGO_URL_HERE" alt="Company Logo">
          <div class="af-title">Your Company</div>
        </div>
        <div class="af-desc">
          Современные цифровые решения. Надёжность, скорость и фокус на будущем.
        </div>

        <div class="af-socials">
          <a class="af-social" href="#" aria-label="Instagram">
            <svg viewBox="0 0 24 24"><path d="M7 2h10a5 5 0 0 1 5 5v10a5 5 0 0 1-5 5H7a5 5 0 0 1-5-5V7a5 5 0 0 1 5-5z"/></svg>
          </a>
          <a class="af-social" href="#" aria-label="Facebook">
            <svg viewBox="0 0 24 24"><path d="M22 12a10 10 0 1 0-11.6 9.9v-7H7.9V12h2.5V9.8c0-2.5 1.5-3.9 3.8-3.9z"/></svg>
          </a>
          <a class="af-social" href="#" aria-label="Telegram">
            <svg viewBox="0 0 24 24"><path d="M21.8 4.6 2.9 11.9c-1.3.5-1.3 1.2-.2 1.6z"/></svg>
          </a>
        </div>
      </div>

      <!-- CONTACT -->
      <div class="af-section">
        <h4>Контакты</h4>
        <div class="af-list">
          <span>📍 Ташкент, Узбекистан</span>
          <span>📞 +998 (90) 000-00-00</span>
          <span>✉️ <a href="mailto:info@company.uz">info@company.uz</a></span>
        </div>
      </div>

      <!-- LINKS -->
      <div class="af-section">
        <h4>Навигация</h4>
        <div class="af-list">
          <a href="#">О нас</a>
          <a href="#">Услуги</a>
          <a href="#">Контакты</a>
          <a href="#">Политика конфиденциальности</a>
        </div>
      </div>
    </div>

    <div class="af-bottom">
      <span>© 2026 Your Company. Все права защищены.</span>
      <span>Сделано для будущего</span>
    </div>
  </div>
</footer>
<!-- === END FOOTER === -->
